extends layout

block content
    h1 = article.title
    h5 Written by #{author}
    p = article.body
    hr
    a.btn.btn-default(href="/article/edit/"+article._id) EDIT
    a.btn.btn-danger.delete-article (href = '#' , data-id=atricle._id) Delete
